<div class="contents row">
    <%= form_tag("/articles/#{@article.id}", method: :patch ) do %>
      <h3>
        編集する
      </h3>
      <textarea cols="30" name="text" placeholder="text" rows="10"><%= @article.content %></textarea>
      <input type="submit" value="SENT">
    <% end %>
  </div>